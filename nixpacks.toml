[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = [
  "npm install -g pnpm",
  "pnpm install"
]

[phases.build]
cmds = [
  "pnpm -r --filter @paperclipai/shared --filter @paperclipai/db --filter @paperclipai/adapter-utils build",
  "pnpm -r --filter @paperclipai/adapters/* build",
  "pnpm -r --filter @paperclipai/ui build",
  "pnpm -r --filter @paperclipai/server build"
]

[start]
cmd = "cd server && node dist/index.js"
